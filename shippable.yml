language: python
python:
  - 2.7
install: pip install --use-mirrors -r requirements.txt
before_script: psql -c 'create database foodb;' -U postgres -w
script: python pgsqlsample.py
after_script: psql -c 'drop database if exists foodb;' -U postgres -w
